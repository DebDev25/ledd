_base_: ../default.yaml
stage: baseline
baseline: {name: resnet50, pretrained: true}
augment: {enabled: true}
loss: {supcon_weight: 0.0, band_entropy_weight: 0.0}
train: {epochs: 10, batch_size: 64, lr_backbone: 1.0e-4, lr_new: 1.0e-3, ckpt_dir: runs/base_resnet50}
