_base_: ../default.yaml
stage: baseline
baseline: {name: npr, grid: 2, width: 64}
augment: {enabled: true}
loss: {supcon_weight: 0.0, band_entropy_weight: 0.0}
train: {epochs: 15, batch_size: 128, lr_backbone: 1.0e-3, lr_new: 1.0e-3, ckpt_dir: runs/base_npr}
