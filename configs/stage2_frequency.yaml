_base_: default.yaml
stage: frequency
loss:
  supcon_weight: 0.0
  band_entropy_weight: 0.0  # ablation: set 0.05 to pretrain with regularizer
train:
  epochs: 15
  batch_size: 256           # tiny model, big batches fine
  lr_new: 1.0e-3
  ckpt_dir: runs/stage2_frequency
